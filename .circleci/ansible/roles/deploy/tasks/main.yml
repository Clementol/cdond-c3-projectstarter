---
- name: "extract the zipped artifact into the EC2 instance"
  become: yes
  shell: |
    tar xzvf artifact.tar.gz
- name: "Start app"
  become: yes
  shell:  |
    npm install
    pm2 stop default
    pm2 start npm -- start